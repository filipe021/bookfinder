# Configura��o do Banco de Dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/bookfinder?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Nicolas
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Dialeto do Hibernate para MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Mostrar as consultas SQL no console
spring.jpa.show-sql=true

# Atualiza automaticamente o banco de dados (use "update" para desenvolvimento, "none" para produ��o)
spring.jpa.hibernate.ddl-auto=update